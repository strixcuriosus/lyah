square x = x * x
atleast y thresh = if y > thresh
                    then y
                    else 0

stuck = "inert by definition"
cat = "fe" ++ "line"
catsound = ['m', 'e'] ++ ['o', 'w']

push item stack = item : stack
pop stack = tail stack
